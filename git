git status // git file status check
git add -A // A mode change
git commit -m // File commit with comment
git log // show git log
q + ; // terminal off
git reset [serial] --hard // serial의 시점까지 이동하고 이전 파일 삭제
git revert [serial] // 되돌아가지만 시점은 삭제하지 않는다
:wq // git 상태 저장
git branch [branch_name] // branch_name의 branch를 만들고 넘어간다
git branch // branch 목록확인
git checkout [branch_name] // branch_name의 branch로 넘어간다
git merge [branch_name] // 변화를 가져올 branch_name을 입력
git rebase [branch_name] //소스트리를 합친다(재배치)
git branch -D [branch_name] //branch_name을 삭제

